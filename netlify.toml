[build]
  functions = "netlify/functions"
  publish = "."
  command = "npm install @treeee/youtube-caption-extractor"

[build.environment]
  NODE_VERSION = "18"
  NODE_ENV = "production"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["@treeee/youtube-caption-extractor"]

# Configuration spécifique pour la fonction subtitles
[functions."subtitles"]
  timeout = 30
  memory = 1024
  node_bundler = "esbuild"
  external_node_modules = ["@treeee/youtube-caption-extractor"]

# Headers de sécurité globaux
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"

# Headers spécifiques pour les functions
[[headers]]
  for = "/.netlify/functions/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
    Access-Control-Allow-Headers = "Content-Type, Authorization, X-Requested-With"
    Access-Control-Allow-Methods = "GET, POST, OPTIONS"
    Cache-Control = "no-cache, no-store, must-revalidate"

# Configuration des redirections d'erreur
[[redirects]]
  from = "/404"
  to = "/index.html"
  status = 200

# Variables d'environnement
[context.production.environment]
  NODE_ENV = "production"

[context.development.environment]
  NODE_ENV = "development"